如何用 NumPy 实现蒙特卡洛模拟股票价格？
先设定基础参数：初始股价、股价波动的剧烈程度（波动率）、模拟多少个交易日、一共模拟多少条价格走势；
用 np.random.standard_normal() 一次性生成所有走势、所有交易日的随机数，不用写 Python 循环逐个生成，这就是向量化运算的好处；
算出每天的价格涨跌比例，再用 np.cumprod() 算累积乘积，直接得到每一天的股价；
最后在每条走势开头补上初始价格，就得到了完整的模拟股价数据。
import numpy as np

# 参数：初始价格100，年化波动率20%，模拟1年252个交易日，1000条路径
S0 = 100
sigma = 0.2
days = 252
n_paths = 1000

# 生成所有路径的每日随机涨跌幅
daily_rand = np.random.standard_normal((n_paths, days))
daily_change = sigma * daily_rand / np.sqrt(252)

# 累积相乘得到每天的股价
price_paths = S0 * np.cumprod(1 + daily_change, axis=1)
# 在开头补上初始价格
price_paths = np.hstack([np.full((n_paths, 1), S0), price_paths])

什么情况下该用 order='F' 的内存布局？
适合用 order='F' 的情况：
    主要按列做计算的时候，比如经常要对每一列求和、算平均值、做统计。如果数据按列连续存，电脑读取数据时更顺畅，不用跳着找内存地址，计算速度就更快。反过来如果主要按行计算，用默认的 C 顺序就好。
调用专业矩阵计算工具的时候很多做矩阵乘法、矩阵分解的底层计算库，本身是按列优先的规则写的。如果传进去的数组本身就是 F 顺序，库就不用额外转换数据格式，运行起来会更快一点。
数据行数特别多、列数少，且以列操作为主，比如几千行几列的数据，大部分操作都是对整列处理，这时候用列优先存储，每一列的数据都挨在一起，读写和计算的效率都会更高。
